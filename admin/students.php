<?php require_admin();$search=trim($_GET['search']??'');$rows=$search!==''?db_all('SELECT s.*,COUNT(c.id) complaint_count FROM students s LEFT JOIN complaints c ON c.student_id=s.id WHERE s.name LIKE ? OR s.student_id LIKE ? OR s.email LIKE ? GROUP BY s.id ORDER BY s.created_at DESC','sss',["%$search%","%$search%","%$search%"]):db_all('SELECT s.*,COUNT(c.id) complaint_count FROM students s LEFT JOIN complaints c ON c.student_id=s.id GROUP BY s.id ORDER BY s.created_at DESC');$pageTitle='Students';require_once __DIR__.'/../includes/header.php'; ?>
<div class="row g-4"><div class="col-lg-3"><?php require __DIR__.'/_sidebar.php'; ?></div><div class="col-lg-9"><div class="dashboard-header mb-4"><div class="small-muted">ADMINISTRATION</div><h1 class="section-title mb-0">Registered students</h1></div><div class="card mb-4"><div class="card-body"><form class="d-flex gap-2"><input class="form-control" name="search" value="<?=e($search)?>" placeholder="Search name, student ID, or email"><button class="btn btn-outline-primary">Search</button></form></div></div><div class="card"><div class="table-responsive"><table class="table"><thead><tr><th>Student</th><th>Student ID</th><th>Email</th><th>Complaints</th><th>Joined</th></tr></thead><tbody><?php if(!$rows): ?><tr><td colspan="5"><div class="empty-state">No students found.</div></td></tr><?php else: foreach($rows as $row): ?><tr><td class="fw-semibold"><?=e($row['name'])?></td><td><?=e($row['student_id'])?></td><td><?=e($row['email'])?></td><td><?= (int)$row['complaint_count'] ?></td><td class="small-muted"><?=e(date('d M Y',strtotime($row['created_at'])))?></td></tr><?php endforeach; endif; ?></tbody></table></div></div></div></div>
<?php require_once __DIR__.'/../includes/footer.php'; ?>
